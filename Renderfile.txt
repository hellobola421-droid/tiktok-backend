services:
  - type: web
    name: tt-downloader-backend
    env: node
    buildCommand: npm install && apt-get update && apt-get install -y python3-pip && pip3 install yt-dlp
    startCommand: npm start